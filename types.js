/**
 * Make event types an object instead of a string
 * Define a json object key that represents an event type and import it
 */
export const TYPES = {
    PostCreated: 'PostCreated',
    CommentCreated: 'CommentCreated',
    CommentModerated: 'CommentModerated',
    CommentVoted: 'CommentVoted'
};